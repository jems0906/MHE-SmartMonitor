services:
  - type: web
    name: mhe-smartmonitor
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 4 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHONDONTWRITEBYTECODE
        value: "1"
